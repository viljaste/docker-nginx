nginx:
  image: simpledrupalcloud/nginx:dev
  hostname: nginx
  ports:
    - "80:80"
    - "443:443"
  volumes_from:
    - nginxdata
nginxdata:
  image: simpledrupalcloud/data:dev
  hostname: nginxdata
  volumes:
    - /nginx/data
