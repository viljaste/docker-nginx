nginx:
  image: simpledrupalcloud/nginx:dev
  hostname: nginx
  ports:
    - "80:80"
    - "443:443"
  volumes_from:
    - nginxdata
  environment:
    - SERVER_NAME=localhost
nginxdata:
  image: simpledrupalcloud/data:dev
  hostname: nginxdata
  volumes:
    - /nginx
